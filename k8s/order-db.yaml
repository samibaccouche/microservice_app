apiVersion: apps/v1
kind: Deployment
metadata:
  name: mongo-order
  namespace: micro-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mongo-order
  template:
    metadata:
      labels:
        app: mongo-order
    spec:
      containers:
      - name: mongo-order
        image: mongo:4.4
        ports:
        - containerPort: 27017
---
apiVersion: v1
kind: Service
metadata:
  name: mongo-order
  namespace: micro-app
spec:
  selector:
    app: mongo-order
  ports:
    - protocol: TCP
      port: 27017
      targetPort: 27017
