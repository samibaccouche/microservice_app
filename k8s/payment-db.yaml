apiVersion: apps/v1
kind: Deployment
metadata:
  name: mongo-payment
  namespace: micro-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mongo-payment
  template:
    metadata:
      labels:
        app: mongo-payment
    spec:
      containers:
      - name: mongo-payment
        image: mongo:4.4
        ports:
        - containerPort: 27017
---
apiVersion: v1
kind: Service
metadata:
  name: mongo-payment
  namespace: micro-app
spec:
  selector:
    app: mongo-payment
  ports:
    - protocol: TCP
      port: 27017
      targetPort: 27017
